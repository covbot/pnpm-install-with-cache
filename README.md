# Install pnpm dependencies with cache 📦

Speed up pnpm installation by caching pnpm store.

## Usage

```yaml
- name: Install pnpm dependencies (with cache)
  uses: covbot/pnpm-install-with-cache@v1
```
